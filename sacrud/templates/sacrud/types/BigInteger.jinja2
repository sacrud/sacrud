{% set value=getattr(obj, col.name) %}
<input type="number"
       class="sacrud_biginteger {{ css_styles }}"
       id="sacrud_biginteger_{{ col.name }}"
       name="{{ col.name }}"
       value="{{ value }}"
       />

{% set value=getattr(obj, col.name) %}
<input type="text"
       class="sacrud_datetime {{ css_styles }}"
       id="sacrud_datetime_{{ col.name }}"
       name="{{ col.name }}"
       value="{{ value }}"
       />

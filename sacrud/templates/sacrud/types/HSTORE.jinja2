{% set value=getattr(obj, col.name) %}
<input type="text"
       class="sacrud_hstore {{ css_styles }}"
       id="sacrud_hstore_{{ col.name }}"
       name="{{ col.name }}"
       value="{{ value }}"
       />

{% set value=getattr(obj, col.name) %}
<textarea class="sacrud_unicode_text {{ css_styles }}"
          id="sacrud_unicode_text_{{ col.name }}"
          name="{{ col.name }}">
  {{ value }}
</textarea>

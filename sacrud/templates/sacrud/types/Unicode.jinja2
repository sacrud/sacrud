{% set value=getattr(obj, col.name) %}
<input type="text"
       class="sacrud_unicode {{ css_styles }}"
       id="sacrud_unicode_{{ col.name }}"
       name="{{ col.name }}"
       value="{{ value }}"
       />

{% set value=getattr(obj, col.name) %}
<input type="number"
       class="sacrud_numeric {{ css_styles }}"
       id="sacrud_numeric_{{ col.name }}"
       name="{{ col.name }}"
       value="{{ value }}"
       />

<select class="sacrud_select {{ css_styles }}"
        id="sacrud_select_{{ col.name }}" name="{{ col.name }}">
  {% for x in col.type.enums %}
    <option value='{{ x }}'
    {% if obj %}
      {% if getattr(obj, col.name)==x %}selected{% endif %}
    {% endif %}>
      {{ x.__repr__() }}
    </option>
  {% endfor %}
</select>

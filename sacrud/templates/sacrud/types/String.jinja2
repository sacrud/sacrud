{% set value=getattr(obj, col.name) %}
<input type="text"
       class="sacrud_string {{ css_styles }}"
       id="string_{{ col.name }}"
       name="{{ col.name }}"
       value="{{ value }}"
       />

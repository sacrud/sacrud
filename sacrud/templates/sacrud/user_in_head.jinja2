{% if request.user %}
  Hello <a href="{{ request.route_url('ziggurat.routes.sign_out') }}">{{ request.user }} (Logout)</a>
{% else %}
  <a href="{{ request.route_url('login') }}">Login</a>
{% endif %}
